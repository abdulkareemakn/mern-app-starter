---
title: Installation
description: Install dependencies, generate the Better Auth secret, start MongoDB, and open the app.
---

TODO: Introduce the page — the end state is a running app at http://localhost:3000.

## Install dependencies

```sh
pnpm install
```

TODO: Note anything worth knowing about the workspace install.

## Create your environment file

<include>/_snippets/environment-setup.mdx</include>

TODO: Add anything specific to your course or deployment, for example using an existing
`MONGODB_URI` instead of the local container.

## Start MongoDB

```sh
pnpm db:up
```

TODO: Explain that this runs `compose.db.yaml`, creates a named volume, and publishes
port 27017 on the loopback address only. Also explain the `MONGODB_URI` alternative for
an existing MongoDB or Atlas deployment.

## Run the client and server

```sh
pnpm dev
```

TODO: Explain that `pnpm dev` watches the client, the server, and the docs app in
parallel, and list the ports (3000, 3001, and 4321 for the docs site).

## Verify the setup

TODO: Tell the reader to open http://localhost:3000, create an account, and use
**Test protected API**. Remind them to use `localhost` consistently because `127.0.0.1`
is a different origin for cookies.

## Next steps

TODO: Link to `/getting-started/project-structure`.
