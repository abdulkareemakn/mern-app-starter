---
title: Architecture
description: How the React client, Express API, and shared contracts package fit together, and why the boundaries sit where they do.
---

TODO: Open with a system diagram or a short ASCII sketch of the three packages and
MongoDB, then describe the request path at a high level.

<CardGroup cols={2}>
  <Card title="Request lifecycle" href="/architecture/request-lifecycle" icon="route">
    Follow one request from the browser to Express and back.
  </Card>
  <Card title="Authentication" href="/architecture/authentication" icon="key-round">
    Sessions, cookies, and how Better Auth fits in.
  </Card>
  <Card title="Shared contracts" href="/architecture/shared-contracts" icon="package">
    Why response types live outside both apps.
  </Card>
  <Card title="Data and models" href="/architecture/data-and-models" icon="database">
    Mongoose on the server, and where it must never run.
  </Card>
</CardGroup>

## Design constraints

TODO: Document the decisions a reader must respect, for example:

- one browser origin, so cookies work without CORS exceptions
- Express owns the API and serves the built client in production
- the shared package contains types only, so it needs no build step
- authentication owns its own collections; application models stay separate

## Boundaries

TODO: Explain what belongs in each package and what must not cross a boundary.
