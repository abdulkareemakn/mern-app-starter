---
title: Data and models
description: Where Mongoose runs, how the connection is established, and where application models belong.
---

TODO: Introduce the page — Mongoose is a server-only concern and the app connects once,
before it accepts traffic.

## The connection

TODO: Walk through `apps/server/src/index.ts`: `mongoose.connect` with a 5 second
`serverSelectionTimeoutMS`, and the decision to exit when startup fails.

```ts
await mongoose.connect(config.mongodbUri, { serverSelectionTimeoutMS: 5000 });
```

## Health checking

TODO: Explain that `GET /api/health` reports readiness from
`mongoose.connection.readyState` and returns 503 until the connection is ready.

## Adding application models

TODO: Explain the convention: add Mongoose schemas and models under
`apps/server/src/models/`, keep them out of the shared package, and never import them
from client code.

## Transactions

TODO: Explain that the default standalone MongoDB container has no multi-document
transactions, and that a replica set or Atlas deployment is required — with the adapter
configured to use the MongoDB client — when atomic writes matter.

## Schemas versus shared types

TODO: Explain that a model document and an API response are different shapes on purpose;
map between them at the route boundary.