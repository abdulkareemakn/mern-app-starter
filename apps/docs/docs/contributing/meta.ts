import { defineMeta } from "blume";

export default defineMeta({
  title: "Contributing",
  icon: "git-branch",
  // Position among the top-level sidebar groups.
  order: 7,
  pages: ["conventions", "template-maintenance"],
});
