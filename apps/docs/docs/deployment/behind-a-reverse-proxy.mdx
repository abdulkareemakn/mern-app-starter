---
title: Behind a reverse proxy
description: Configure TRUST_PROXY and forwarding headers so client addresses and rate limits stay correct.
---

TODO: Introduce the page — proxies change who Express thinks the client is.

## Set TRUST_PROXY

TODO: Explain that `TRUST_PROXY` is a comma-separated list of trusted IPs or CIDRs, that
it stays empty for direct connections, and that only the exact addresses of your own
proxy should be listed.

## Forward client-IP headers

TODO: Explain that the proxy must overwrite forwarded client-IP headers, and that the
server records the resolved address in its own header (`x-mern-client-ip`), which
browser-supplied values cannot bypass.

## Why it matters for rate limiting

TODO: Explain that Better Auth's rate limiting keys on the client address, so a
misconfigured proxy either lets spoofed headers through or puts every user behind the
ingress into one bucket.

## Public URLs

TODO: Remind the reader that `APP_URL` and `BETTER_AUTH_URL` must be the same public
HTTPS origin, and that `APP_PORT` does not change the public URL.

## Verify

TODO: Give a check — read the server logs or add a temporary route that echoes `req.ip`,
then confirm it shows the real client address rather than the proxy's.