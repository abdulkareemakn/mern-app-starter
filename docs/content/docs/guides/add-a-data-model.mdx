---
title: Add a data model
description: Define a Mongoose schema and model under apps/server/src/models/ and use it from a route.
---

TODO: Introduce the guide — when you need application data instead of auth data.

## 1. Create the model

TODO: Show the file to create at `apps/server/src/models/` and a minimal schema:

```ts
import mongoose from "mongoose";

export const Widget = mongoose.model(
  "Widget",
  new mongoose.Schema({ name: { type: String, required: true } }, { timestamps: true }),
);
```

## 2. Use it in a route

TODO: Show reading and writing through the model in a handler, and mapping the document
to a shared response type.

## 3. Keep auth data out

TODO: Repeat the warning: Better Auth manages its own collections through the MongoDB
adapter, so do not create a competing user schema.

## 4. Verify

TODO: Give the check to run, for example starting MongoDB with `pnpm db:up` and hitting
the endpoint.
