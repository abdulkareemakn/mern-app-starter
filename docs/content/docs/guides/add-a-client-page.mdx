---
title: Add a client page
description: Create a TanStack Router file route, reach it in the browser, and link to it from another page.
---

TODO: Introduce the guide — file routes are generated from the file system.

## 1. Create the route file

TODO: Show a new file under `apps/client/src/routes/` and the route it produces:

```tsx
import { createFileRoute } from "@tanstack/react-router";

export const Route = createFileRoute("/widgets")({ component: Widgets });

function Widgets() {
  return <main className="mx-auto max-w-lg p-8">Widgets</main>;
}
```

## 2. Understand generated types

TODO: Explain that `apps/client/src/routeTree.gen.ts` is generated automatically during
development and build, is committed on purpose, and must never be edited by hand.

## 3. Link between pages

TODO: Show a typed `<Link to="/widgets">` and explain the type safety it gives you.

## 4. Load data

TODO: Show fetching a protected endpoint with `fetch`, or using a router loader, and how
the component handles loading and error states.

## 5. Verify

TODO: Give the check to run — visit the route while `pnpm dev` is running, and run
`pnpm typecheck` to confirm route types regenerate cleanly.
