# MERN course starter docs

This documentation site is built with [Zensical](https://zensical.org/). The published
site is built by the repository's GitHub Pages workflow.

```sh
zensical serve
# or validate the production build
zensical build --clean --strict
```

Content lives in `docs/`; navigation is configured in `zensical.toml`.
