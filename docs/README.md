# MERN course starter docs

This is a standalone Fumadocs site. It is intentionally excluded from the root pnpm
workspace and must be developed and deployed separately from the application.

Run development server:

```sh
cd docs
pnpm install
pnpm dev
```

Content lives in `content/docs`. Use each folder's `meta.json` to control sidebar
labels and ordering.
